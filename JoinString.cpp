#include <iostream>
#include<string>
#include<list>
#include<map>
#include<algorithm>
#include<iterator>

using namespace std;

int main(){
    cin.tie(NULL);ios_base::sync_with_stdio(false);
    int N;
    cin>>N;
    list<string>list1;

    for(int i=0;i<N;i++){
        string curr;
        cin>>curr;
        list1.push_back(curr);

    }

list<string>::iterator it;
it = list1.begin();

int index1, index2,lastIndex=0;




return 0;

}
